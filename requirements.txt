# Transformers & Accelerate (matching working venv)
transformers==4.57.3
accelerate==1.12.0
Pillow>=10.0.0

# Qwen-TTS specific logic (downgrade to stable 0.0.5)
qwen-tts==0.0.5
flash-attn==2.8.3

# For reading and writing WAV files (libsndfile frontend)
soundfile>=0.12.1

# For native audio playback (provides ALSA connection for Bobs voice)
sounddevice>=0.4.6

# --- GUI & System ---
# The dashboard for real-time parameter configuration
PyQt5>=5.15.10
